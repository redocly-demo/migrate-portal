{
  "name": "migrate-portal",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "bin": "dist/bin.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild bin.ts --bundle --platform=node --outfile=dist/bin.js"
  },
  "keywords": [],
  "author": "Roman Hotsiy <roman@redocly.com>",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.23.1",
    "js-yaml": "^4.1.0"
  }
}
